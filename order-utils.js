(() => {
  const normalize=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\s+/g,' ').trim();
  const number=v=>Number(String(v||'').replace(/[^\d,.-]/g,'').replace('.','').replace(',','.'))||0;
  function parseWhatsAppOrder(text,products=window.MMS_PRODUCTS||{}){const lines=String(text||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean),result={source:'whatsapp',items:[],paymentStatus:'Pendente',status:'NOVO',warnings:[]};let current=null;for(const line of lines){const clean=normalize(line),value=line.split(':').slice(1).join(':').trim();if(/^(cliente|nome):/.test(clean))result.customerName=value;else if(/^(telefone|whatsapp):/.test(clean))result.customerPhone=value.replace(/\D/g,'');else if(/^(para entregar|data):/.test(clean)){const m=line.match(/(\d{2})\/(\d{2})\/(\d{4})(?:.*?(\d{2}:\d{2}))?/);if(m){result.date=`${m[3]}-${m[2]}-${m[1]}`;result.time=m[4]||''}}else if(/^(endereco|endereço):/.test(clean))result.address=value;else if(/^(pagamento|forma de pagamento):/.test(clean))result.paymentMethod=value;else if(/^(subtotal):/.test(clean))result.subtotal=number(value);else if(/^(total):/.test(clean))result.total=number(value);else if(/^(frete|taxa de entrega):/.test(clean)){result.deliveryType=/retirada/i.test(value)?'Retirada':'Entrega';result.deliveryFee=/confirmar/i.test(value)?null:number(value)}else if(/^(item|produto):/.test(clean)||/^\d+\s*[x×-]/i.test(line)){const quantity=Number(line.match(/(\d+)\s*[x×-]/i)?.[1]||1),name=value||line.replace(/^\d+\s*[x×-]\s*/i,'');const match=Object.entries(products).find(([,p])=>normalize(name).includes(normalize(p.name))||normalize(p.name).includes(normalize(name)));current={productId:match?.[0]||'',name:match?.[1].name||name,quantity,options:{},notes:''};result.items.push(current);if(!match)result.warnings.push(`Produto não reconhecido: ${name}`)}else if(current&&/^(opcoes|opções|tamanho|acabamento|massa|recheio):/.test(clean))current.options.raw=value;else if(current&&/^(observacao|observação):/.test(clean))current.notes=value;else if(/^(observacao geral|observação geral|notas):/.test(clean))result.notes=value}if(!result.customerName)result.warnings.push('Cliente não identificado');if(!result.date)result.warnings.push('Data não identificada');if(!result.items.length)result.warnings.push('Nenhum produto identificado');result.total=result.total||result.subtotal||0;return result}
  const csvCell=v=>`"${String(v??'').replace(/"/g,'""')}"`;
  function ordersToCSV(orders){return ['Pedido,Cliente,Telefone,Data,Horário,Recebimento,Pagamento,Status pagamento,Status,Total',...orders.map(o=>[o.orderNumber,o.customerName,o.customerPhone,o.date,o.time,o.deliveryType,o.paymentMethod,o.paymentStatus,o.status,o.total].map(csvCell).join(','))].join('\r\n')}
  window.MMSOrderUtils={normalize,parseWhatsAppOrder,ordersToCSV};
})();
